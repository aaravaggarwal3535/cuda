# Kernels


## vector add 1D
### cudaMalloc
```
used to allocate memory on the gpu
```
### cudaMemcpy
```
used to copy variable values from devise to host and host to device
```
### cudaFree
```
used to free memory of the variables that are no longer in use on the gpu prevent memory leak
```

## vector add 3D
```
all are same as 1D but the calculation of the thread location is just diffrent here
```

## matmul
```
same as above but with multiplication logic
```