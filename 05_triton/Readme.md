# Triton

CUDA --> scaler program(each kernel on each thread) + blocked thread(threads can work toogether)
Triton --> blocked program(in this the code is breaded down onto block level stuff) + scaler thread(these are handled by the compiler itself)

